scheme("flo")

export_options({
  method: "app-store",
  provisioningProfiles: {
    "com.penerbangwalet.flo" => "match AppStore com.penerbangwalet.flo" # is this necessary?
  }
})

output_directory("./fastlane/builds")

include_bitcode(false)
include_symbols(false)
